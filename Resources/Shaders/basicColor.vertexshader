#version 330 core
layout (location = 0) in vec2 aPos;
layout (location = 1) in vec3 aColor;

out vec3 Color;
uniform vec2 ScreenPos;
uniform vec2 Scale;

void main()
{
    gl_Position = vec4(ScreenPos.x + Scale.x * aPos.x, ScreenPos.y + Scale.y * aPos.y , 0.0, 1.0);
    Color = aColor;
}