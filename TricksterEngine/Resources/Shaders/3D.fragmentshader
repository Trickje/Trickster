#version 330 core

in vec3 vs_color;
in vec2 vs_texcoord;

uniform sampler2D ourTexture;

out vec4 fs_color;

void main()
{
    vec4 texColor = texture(ourTexture, vs_texcoord);
	fs_color = texColor;
}