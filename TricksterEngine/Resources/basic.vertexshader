#version 330 core
layout (location = 0) in vec2 aPos;

out vec2 TexCoord;
uniform vec2 ScreenPos;

void main()
{
    gl_Position = vec4(ScreenPos.x + aPos.x, ScreenPos.y + aPos.y , 0.0, 1.0);
    TexCoord = aPos;
}